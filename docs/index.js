import React from 'react';
import ReactDOM from 'react-dom'; // eslint-disable-line import/no-extraneous-dependencies
import App from './App';

const mountNode = document.getElementById('app'); // eslint-disable-line
ReactDOM.render(<App />, mountNode); // eslint-disable-line
